<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:banner="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Liratele">

        <!-- Actividad de Bienvenida -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="false" />

        <!-- Actividades del menú -->
        <activity android:name=".JuegoVocalesActivity" />
        <activity android:name=".FormarPalabrasActivity"/>
        <activity android:name=".CuentosDivertidosActivity"/>
        <activity android:name=".DesafiosLiraActivity"/>
        <activity android:name=".ProximamenteActivity"/>

        <!-- 🔧 AGREGA ESTA LÍNEA -->
        <activity android:name=".SeleccionDificultadActivity" />

        <!-- Menú Principal -->
        <activity
            android:name=".MainMenuActivity"
            android:exported="false"
            android:theme="@style/Theme.Liratele"/>

        <!-- Actividad Principal (con QR) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>